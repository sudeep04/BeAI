import 'package:flutter/material.dart';

class DataVault extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: new Text("Data Vault"),
    );
  }

}